{% extends 'base.html.twig' %}

{% block title %}Réinitialisation mot de passe
{% endblock %}

{% block main %}
{# <div class="container my-5">
		{% for flash_error in app.flashes('reset_password_error') %}
			<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
		{% endfor %}
		<h1>Réinitialisez votre mot de passe</h1>

		{{ form_start(requestForm) }}
		{{ form_row(requestForm.email) }}
		<div>
			<small>
				Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
			</small>
		</div>

		<button class="btn btn-primary">Envoyer l'email de réinitialisation du mot de passe</button>
		{{ form_end(requestForm) }}
</div> #}
<!-- Background image -->
	<div id="intro" class="bg-image bg_request shadow-2-strong">
		<div class="d-flex align-items-center">
			<div class="container">				
				<div class="row justify-content-center">					
					<div class="col-md-8 col-xl-5 my-5">
						{{ form_start(requestForm, {
							'attr': { 
							'class': "mask-custom rounded-5 shadow-5-strong p-5",
							'id': 'page-form'
								}
							})
						}}
						<h1 class="text-center">Réinitialisez votre mot de passe</h1>
						{% for flash_error in app.flashes('reset_password_error') %}
							<div class="alert alert-danger" role="alert">{{ flash_error }}</div>
						{% endfor %}
						{{ form_row(requestForm.email) }}
							<div>
								<small>
									Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
								</small>
							</div>

							<div class="text-center mt-2">
								<button class="btn btn-primary my-2">Envoi de l'email de réinitialisation</button>
							</div>
						{{ form_end(requestForm) }}						
					</div>
				</div>
			</div>
		</div>
	</div>
<!-- Background image -->
{% endblock %}
